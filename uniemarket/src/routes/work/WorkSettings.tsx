import { WorkStub } from "./WorkStub";

/** /work/settings (admin) — STK ngân hàng, Momo, ảnh QR, tên shop. */
export function WorkSettings() {
  return (
    <WorkStub
      title="Cài đặt"
      description="Thông tin nhận thanh toán (ngân hàng, Momo, QR) và thương hiệu shop."
    />
  );
}
