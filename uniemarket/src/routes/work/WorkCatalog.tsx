import { WorkStub } from "./WorkStub";

/** /work/catalog (admin) — CRUD danh mục + sản phẩm, upload ảnh, option builder. */
export function WorkCatalog() {
  return (
    <WorkStub
      title="Danh mục & sản phẩm"
      description="Tạo/sửa danh mục, sản phẩm item & dịch vụ, tải ảnh, cấu hình option giá."
    />
  );
}
