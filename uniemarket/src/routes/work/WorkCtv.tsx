import { WorkStub } from "./WorkStub";

/** /work/ctv (admin) — duyệt đơn ứng tuyển CTV + danh sách CTV. */
export function WorkCtv() {
  return (
    <WorkStub
      title="Cộng tác viên"
      description="Duyệt đơn ứng tuyển (duyệt = nâng vai trò lên CTV) và quản lý CTV."
    />
  );
}
